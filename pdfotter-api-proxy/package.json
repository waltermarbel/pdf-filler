{
  "name": "pdfotter-api-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "A universal, secure Cloudflare Worker proxy for the PDF Otter API.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.15.0"
  }
}
